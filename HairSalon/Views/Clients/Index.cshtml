@{
  Layout = "_Layout";
}

<h2>Clients</h2>
@if (@Model.Count > 0)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Client Name</th>
                <th>Client Contact</th>
                <th>Details</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var client in @Model)
            {
                <tr>
                    <td>@client.GetFirstName() @client.GetLastName()</td>
                    <td>@client.GetPhoneNumber()</td>
                    <td><a href="/clients/@client.GetId()">Details</a></td>
                </tr>
            }
        </tbody>
    </table>
}
<a href="/clients/delete">Delete All Clients</a>
<a href="/">Return Home</a>
